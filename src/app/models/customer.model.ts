import {initializeOrderTimingsState} from './order-timings.model';

export default class CustomerModel {
  firstName: string;
  lastName: string;
  email: string;
  phone: string;
  isActive: boolean;
}

